# Kubenetes V1

# Helpful commands

List all port

`sudo lsof -i -P -n | grep LISTEN`

`sudo iptables -L -n -v`

swapoff -a

sudo iptables -F
sudo iptables -X
sudo iptables -t nat -F
sudo iptables -t nat -X
sudo iptables -t mangle -F
sudo iptables -t mangle -X
sudo iptables -P INPUT ACCEPT
sudo iptables -P FORWARD ACCEPT
sudo iptables -P OUTPUT ACCEPT

```latex
sudo iptables -A INPUT -p tcp --dport 80 -j ACCEPT
sudo iptables -A INPUT -p tcp --dport 443 -j ACCEPT
sudo iptables -A INPUT -p tcp --dport 6443 -j ACCEPT
sudo iptables -A INPUT -p tcp --dport 10250 -j ACCEPT
sudo iptables -A INPUT -p tcp --dport 33073 -j ACCEPT
sudo iptables -A INPUT -p tcp --dport 10000 -j ACCEPT
sudo iptables -A INPUT -p tcp --dport 33080 -j ACCEPT
sudo iptables -A INPUT -p udp --dport 3478 -j ACCEPT
sudo iptables -A INPUT -p udp --dport 49152:65535 -j ACCEPT

sudo firewall-cmd --permanent --add-port=6443/tcp
sudo firewall-cmd --permanent --add-port=10250/tcp
sudo firewall-cmd --permanent --add-port=80/tcp
sudo firewall-cmd --permanent --add-port=443/tcp
sudo firewall-cmd --reload
sudo iptables-save > /etc/sysconfig/iptables
```

```latex
kubeadm reset
systemctl stop kubelet
rm -rf /var/lib/cni/
rm -rf /var/lib/kubelet/*
rm -rf /run/flannel
rm -rf /etc/cni/
ifconfig cni0 down
brctl delbr cni0
ifconfig flannel.1 down
ip link delete cni0
ip link delete flannel.1
##

sudo yum remove kubeadm kubelet kubectl kubernetes-cni
sudo yum autoremove
sudo rm -rf /etc/kubernetes/
rm -rf ~/.kube/
sudo yum remove purge docker-ce docker-ce-cli containerd.io
sudo yum remove  autoremove
rm -rf /var/lib/cni/
rm -rf /var/lib/kubelet/*
rm -rf /run/flannel
rm -rf /etc/cni/
ifconfig 

ifconfig cni0 down
brctl delbr cni0
ifconfig flannel.1 down
ip link delete cni0
ip link delete flannel.1
```

# Database Storage Mangement

| Resource (Name Space) | Sub Resources | Volume Storages | Notes (Current) |
| --- | --- | --- | --- |
| Database |  |  |  |
|  | Whiteboard Postgres | 2Gi | 1Gi |
|  | PgAdmin | 512Mi | 2Gi |
| Habor |  |  |  |
|  | Redis | 150Mi | 1Gi |
|  | registry | 4Gi | 4Gi |
|  | JobServices | 150Mi | 1Gi |
|  | Trivy | 1GI | 1Gi |
|  | Db | 1Gi | 1Gi |
| MinIO |  |  |  |
|  | Deployment | 10Gi | 5Gi |
|  | PostgreSql | 512Mi | 1Gi |
| N8n |  |  |  |
|  | PostgreSql | 512Mi | 1Gi |
|  | Main | 100Mi | 1Gi |
| Portainer | Portainer | 150Mi | 1Gi |
|  |  |  |  |
|  |  |  |  |
|  |  |  |  |

# Setup Netbird

```latex
# Allow incoming TCP traffic on ports 80, 443, 33073, 10000, and 33080
sudo iptables -A INPUT -p tcp --dport 80 -j ACCEPT
sudo iptables -A INPUT -p tcp --dport 443 -j ACCEPT
sudo iptables -A INPUT -p tcp --dport 33073 -j ACCEPT
sudo iptables -A INPUT -p tcp --dport 10000 -j ACCEPT
sudo iptables -A INPUT -p tcp --dport 33080 -j ACCEPT

# Allow incoming UDP traffic on port 3478
sudo iptables -A INPUT -p udp --dport 3478 -j ACCEPT

# Allow incoming UDP traffic on ports 49152-65535
sudo iptables -A INPUT -p udp --dport 49152:65535 -j ACCEPT

# Save the iptables rules (this is crucial for CentOS with iptables)
sudo iptables-save > /etc/sysconfig/iptables

export NETBIRD_DOMAIN=<WORKER2_VPS_PUBLIC_IP>; curl -fsSL https://github.com/netbirdio/netbird/releases/0.25/download/getting-started-with-zitadel.sh | bash
Username: <ZITADEL_ADMIN_USER>
Password: <ZITADEL_ADMIN_PASSWORD>

```

```latex
curl -fsSL https://pkgs.netbird.io/install.sh | sh
netbird up --allow-server-ssh --management-url https://nccquynhon.edu.vn --admin-url https://nccquynhon.edu.vn --setup-key <NETBIRD_SETUP_KEY>
```

# Resources

| Server | CPUs | Mem | Disk | IP | OS | Description |
| --- | --- | --- | --- | --- | --- | --- |
| Master | 4 | 8 | 20 | <MASTER_VPS_PUBLIC_IP> | **CentOS-7.9** | - HCM
[https://my.cloudfly.vn/cloud/server/c1ca99db-f7f2-48aa-9748-2a2d19da88db](https://my.cloudfly.vn/cloud/server/c1ca99db-f7f2-48aa-9748-2a2d19da88db) |
| Worker 1 | 4 | 8 | 20 | <VPS_PUBLIC_IP> | **Ubuntu-22.04_64bit** | - HN
[https://my.cloudfly.vn/cloud/server/2121ea80-18b1-4b8b-a86b-899b7561cf6c](https://my.cloudfly.vn/cloud/server/2121ea80-18b1-4b8b-a86b-899b7561cf6c) |
| VPN | 1 | 2 | 40 | <WORKER2_VPS_PUBLIC_IP> | **Ubuntu-22.04_64bit** | - HCM 
[https://my.cloudfly.vn/cloud/server/74f3bd4b-3718-4a0e-a855-0bbe243b1acf](https://my.cloudfly.vn/cloud/server/74f3bd4b-3718-4a0e-a855-0bbe243b1acf) |

### Traefik

```
<TRAEFIK_PASSWORD>
```

### Portainer

```
username: admin
password: <PORTAINER_PASSWORD>
```

### Longhorn

```
username: admin
password: <LONGHORN_PASSWORD>
```

### Minio

```
pass: <MINIO_PASSWORD>
```

### Harbor

```
password: <HARBOR_PASSWORD>

cicd
<HARBOR_CICD_PASSWORD>
```

---

# Pre Setup

```
[Master]
ssh -o PasswordAuthentication=yes -o PubkeyAuthentication=no root@<MASTER_VPS_PUBLIC_IP>
<SSH_MASTER_PASSWORD>

[Worker1]
ssh -o PasswordAuthentication=yes -o PubkeyAuthentication=no root@<VPS_PUBLIC_IP>
<SSH_WORKER1_PASSWORD>

[Worker2]
ssh -o PasswordAuthentication=yes -o PubkeyAuthentication=no root@<WORKER2_VPS_PUBLIC_IP>
<SSH_WORKER2_PASSWORD>
```

## CentOS - Every Nodes

```
cat >>/etc/hosts<<EOF
<MASTER_VPS_PUBLIC_IP>   vps-master 
<VPS_PUBLIC_IP>  vps-worker-1
<WORKER2_VPS_PUBLIC_IP> vps-worker-2
EOF

echo "ssh-rsa " > .ssh/authorized_keys

sudo iptables -A INPUT -p tcp --dport 6443 -j ACCEPT
sudo iptables -A INPUT -p tcp --dport 10250 -j ACCEPT
sudo iptables-save > /etc/sysconfig/iptables
```

```
sudo hostnamectl set-hostname "vps-master"
sudo iptables -A INPUT -p tcp --dport 80 -j ACCEPT
sudo iptables -A INPUT -p tcp --dport 443 -j ACCEPT
#
sudo hostnamectl set-hostname "vps-worker-1"
#
sudo hostnamectl set-hostname "vps-worker-2"
```

```
sudo yum update -y
sudo yum install -y git vim wget curl dnf iscsi-initiator-utils tar jq net-tools
swapoff -a
sudo sed -i '/ swap / s/^\(.*\)$/#\1/g' /etc/fstab

# [https://kubernetes.io/docs/setup/production-environment/container-runtimes/](https://kubernetes.io/docs/setup/production-environment/container-runtimes/)
cat <<EOF | sudo tee /etc/sysctl.d/k8s.conf
net.ipv4.ip_forward = 1
EOF

sudo modprobe overlay
sudo modprobe br_netfilter
cat <<EOF | sudo tee /etc/modules-load.d/containerd.conf
overlay
br_netfilter
EOF
sudo sysctl --system

sudo dnf -y install dnf-plugins-core
sudo dnf config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo
sudo dnf update 
sudo dnf install -y containerd
systemctl daemon-reload
systemctl enable --now containerd
sudo mkdir /data/
sudo mkdir /data/root/
sudo mkdir /data/state/
sudo mkdir /data/longhorn/
sudo mkdir -p /etc/containerd
containerd config default | sudo tee /etc/containerd/config.toml
sed -i 's|^root = "/var/lib/containerd"|root = "/data/root"|' /etc/containerd/config.toml
sed -i 's|^state = "/run/containerd"|state = "/data/state"|' /etc/containerd/config.toml
vi /etc/containerd/config.toml
```

```
sudo systemctl restart containerd 

### INSTALL Kubenetes tool
basearch=$(rpm -q -qf "%{arch}" -f /etc/$distro)

cat <<EOF | sudo tee /etc/yum.repos.d/kubernetes.repo
[kubernetes]
name=Kubernetes
baseurl=https://pkgs.k8s.io/core:/stable:/v1.32/rpm/
enabled=1
gpgcheck=1
gpgkey=https://pkgs.k8s.io/core:/stable:/v1.32/rpm/repodata/repomd.xml.key
exclude=kubelet kubeadm kubectl cri-tools kubernetes-cni
EOF

sudo setenforce 0
sudo sed -i 's/^SELINUX=enforcing$/SELINUX=permissive/' /etc/selinux/config
sudo yum install -y kubelet kubeadm kubectl --disableexcludes=kubernetes
sudo systemctl enable --now kubelet

### INSTALL HELM
curl -fsSL -o get_helm.sh https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3
chmod 700 get_helm.sh
./get_helm.sh
```

---

# Setup Cluster

## **In Master Node**

```latex
~~--pod-network-cidr=<K8S_POD_SUBNET> --skip-phases=addon/kube-proxy 
Error netwok~~

sudo kubeadm init --control-plane-endpoint="vps-master:6443" --upload-certs --pod-network-cidr=<K8S_POD_SUBNET> --skip-phases=addon/kube-proxy
kubeadm init --control-plane-endpoint="qn-server:6443" --upload-certs --pod-network-cidr=192.168.0.0/16
```

```
mkdir -p $HOME/.kube
sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
sudo chown $(id -u):$(id -g) $HOME/.kube/config 

# In case commands above not work
sudo chmod -R 777 $HOME/.kube

###
kubectl taint node vps-master node-role.kubernetes.io/control-plane:NoSchedule-

kubectl taint nodes --all node.cilium.io/agent-not-ready=true:NoExecute
```

```
kubectl taint node vps-master node-role.kubernetes.io/control-plane:NoExecute

```

## Worker Nodes

```latex
  kubeadm join vps-master:6443 --token pqz8rs.r5fwk9vqx8tt8ywg \
	--discovery-token-ca-cert-hash sha256:fe3cabb00ecf1a910c253fe40654ec450abef7e51eae2b5e417f51c34d8630ad \
	--control-plane --certificate-key bcb017a3b58398d4850539faad4daf78f1a102f1042a24785d79057d6bc4ebf3

Please note that the certificate-key gives access to cluster sensitive data, keep it secret!
As a safeguard, uploaded-certs will be deleted in two hours; If necessary, you can use
"kubeadm init phase upload-certs --upload-certs" to reload certs afterward.

Then you can join any number of worker nodes by running the following on each as root:

kubeadm join vps-master:6443 --token pqz8rs.r5fwk9vqx8tt8ywg \
	--discovery-token-ca-cert-hash sha256:fe3cabb00ecf1a910c253fe40654ec450abef7e51eae2b5e417f51c34d8630ad 
```

### After Joined Nodes

```
kubectl label nodes vps-worker-1 node-role.kubernetes.io/worker=worker
kubectl label nodes vps-worker-2 node-role.kubernetes.io/worker=worker

```

## Install CNI Plugin (EVERY NODES)

```

CILIUM_CLI_VERSION=$(curl -s https://raw.githubusercontent.com/cilium/cilium-cli/master/stable.txt)

CLI_ARCH=amd64

if [ "$(uname -m)" = "aarch64" ]; then CLI_ARCH=arm64; fi

curl -L --fail --remote-name-all https://github.com/cilium/cilium-cli/releases/download/${CILIUM_CLI_VERSION}/cilium-linux-${CLI_ARCH}.tar.gz{,.sha256sum}

sha256sum --check cilium-linux-${CLI_ARCH}.tar.gz.sha256sum

cilium install --version 1.16.5
cilium status --wait
cilium connectivity test
kubectl delete ns cilium-test

# Romove installed tools
sudo tar xzvfC cilium-linux-${CLI_ARCH}.tar.gz /usr/local/bin
rm cilium-linux-${CLI_ARCH}.tar.gz{,.sha256sum}
```

```

cilium uninstall
```

# Installing Cert Manager

```
helm repo add jetstack https://charts.jetstack.io --force-update
helm install cert-manager jetstack/cert-manager --namespace cert-manager --create-namespace --values values.yml
kubectl apply -f issuer.secret.yml
kubectl apply -f  cluster-issuer.yml
kubectl apply -f certificate.yml
```

# Installing Traefik

```latex
helm repo add traefik https://traefik.github.io/charts
helm repo update
kubectl create ns traefik
kubectl create ns longhorn-system
kubectl apply -f tls.yml
kubectl apply -f auth.middleware.yml
helm install -n traefik traefik traefik/traefik --create-namespace --values values.yml
helm upgrade -n traefik traefik traefik/traefik --values values.yml
watch kubectl -n traefik get pods
```

- **Troubleshoot**
    
    ERR Command error error="command traefik error: error while building entryPoint websecure: error preparing server: error opening listener: listen tcp :443: bind: permission denied"
    
    [https://stackoverflow.com/questions/66138370/permission-problem-w-helm3-installation-of-traefik-on-port-80-hostnetwork](https://stackoverflow.com/questions/66138370/permission-problem-w-helm3-installation-of-traefik-on-port-80-hostnetwork)
    

```
helm uninstall -n traefik traefik
kubectl delete -f tls.yml
kubectl delete -f authentik-forward-auth.yml
kubectl delete ns traefik
kubectl create ns traefik
kubectl apply -f tls.yml
kubectl apply -f authentik-forward-auth.yml
helm install -n traefik traefik traefik/traefik --create-namespace --values values.yml
watch kubectl -n traefik get pods
```

# Install Longhorn

### Master Node

`kubectl apply -f https://raw.githubusercontent.com/longhorn/longhorn/v1.7.2/deploy/prerequisite/longhorn-nfs-installation.yaml`

kubectl apply -f [https://raw.githubusercontent.com/longhorn/longhorn/v1.7.2/deploy/prerequisite/longhorn-iscsi-installation.yaml](https://raw.githubusercontent.com/longhorn/longhorn/v1.4.0/deploy/prerequisite/longhorn-iscsi-installation.yaml)

To cleanup in the future

```
kubectl delete -f https://raw.githubusercontent.com/longhorn/longhorn/v1.7.2/deploy/prerequisite/longhorn-nfs-installation.yaml
```

```
helm repo add longhorn https://charts.longhorn.io
helm repo update
kubectl create ns longhorn-system
helm install longhorn longhorn/longhorn --namespace longhorn-system --create-namespace --version 1.7.2
```

```
helm upgrade longhorn longhorn/longhorn --namespace longhorn-system --values values.yml
```

# Installing Portainer

```
helm repo add portainer https://portainer.github.io/k8s/
helm repo update
kubectl create ns portainer
helm install -n portainer portainer portainer/portainer --values values.yml
watch kubectl get pods -n portainer
```

```
helm upgrade -n portainer portainer portainer/portainer --values values.yml
```

```
helm uninstall -n portainer portainer 
kubectl delete ns portainer
```

# Installing MinIO

```
kubectl create ns minio-system
kubectl apply -f minio-deployment.yml
```

```
kubectl delete -f minio-deployment.yml
kubectl delete ns minio-system

```

# ~~Gitlab~~

We will use our deployed Minio: [https://gitlab.com/gitlab-org/charts/gitlab/-/blob/master/doc/advanced/external-object-storage/minio.md](https://gitlab.com/gitlab-org/charts/gitlab/-/blob/master/doc/advanced/external-object-storage/minio.md)

```
helm repo add gitlab https://charts.gitlab.io/
helm repo update
kubectl create ns gitlab
kubectl apply -f object-storage.secret.yml
helm install gitlab gitlab/gitlab -n gitlab --values values.yml
http://minio-service.minio-system.svc:9000
```

```
helm upgrade gitlab gitlab/gitlab -n gitlab --values values.yml

helm uninstall gitlab -n gitlab
kubectl delete ns gitlab
```

```dart
Registry Accesstoken
<REGISTRY_ACCESSTOKEN>
123qwe
docker login gitlab-registry.dungxbuif.com
docker build -t gitlab-registry.dungxbuif.com/dungxbuif/selfhost .
docker push gitlab-registry.dungxbuif.com/dungxbuif/selfhost
```

[https://docs.gitlab.com/charts/installation/](https://docs.gitlab.com/charts/installation/)

[https://docs.gitlab.com/administration/reference_architectures/](https://docs.gitlab.com/administration/reference_architectures/)

## Required

Deploy riêng

- Postgre
- Redis
- Gitaly
- [MinIO](https://docs.gitlab.com/charts/advanced/external-object-storage/)
- [https://gitlab.com/gitlab-org/charts/gitlab/-/blob/master/examples/objectstorage/registry.s3.yaml](https://gitlab.com/gitlab-org/charts/gitlab/-/blob/master/examples/objectstorage/registry.s3.yaml)
    - **registry.minio.yaml**
        
        ```dart
        s3:
          v4auth: true
          regionendpoint: "https://storage.example.com:9000"
          pathstyle: true
          region: us-east-1
          bucket: registry
          accesskey: ACCESSKEY
          secretkey: SECRETKEY
        
        ```
        

# ~~Bitwaren~~

```
Installation ID: <BITWARDEN_INSTALL_ID>
Installation Key: <BITWARDEN_INSTALL_KEY>
```

```
helm repo add bitwarden https://charts.bitwarden.com/
helm repo update
kubectl create namespace bitwarden
kubectl apply -f bitwarden.secret.yml
helm install bitwarden bitwarden/self-host --namespace bitwarden --values values.yml
```

```
helm upgrade bitwarden bitwarden/self-host --namespace bitwarden --values values.yml

```

# Habor

```latex
helm repo add harbor https://helm.goharbor.io
kubectl create ns harbor
helm install harbor harbor/harbor -n harbor --values values.yml

```

```latex
helm upgrade harbor harbor/harbor -n harbor --values values.yml

helm uninstall harbor harbor/harbor -n harbor 

```

# Whiteboard

```
helm install whiteboard-db oci://registry-1.docker.io/bitnamicharts/postgresql --namespace --create-namespace database --values whiteboard-database.values.yml

```

```

Did you know there are enterprise versions of the Bitnami catalog? For enhanced secure software supply chain features, unlimited pulls from Docker, LTS support, or application customization, see Bitnami Premium or Tanzu Application Catalog. See https://www.arrow.com/globalecs/na/vendors/bitnami for more information.

** Please be patient while the chart is being deployed **

PostgreSQL can be accessed via port 5432 on the following DNS names from within your cluster:

    whiteboard-db-postgresql.database.svc.cluster.local - Read/Write connection

To get the password for "postgres" run:

    export POSTGRES_ADMIN_PASSWORD=$(kubectl get secret --namespace database whiteboard-db-postgresql -o jsonpath="{.data.postgres-password}" | base64 -d)

To get the password for "whiteboard" run:

    export POSTGRES_PASSWORD=$(kubectl get secret --namespace database whiteboard-db-postgresql -o jsonpath="{.data.password}" | base64 -d)

To connect to your database run the following command:

    kubectl run whiteboard-db-postgresql-client --rm --tty -i --restart='Never' --namespace database --image docker.io/bitnami/postgresql:17.2.0-debian-12-r5 --env="PGPASSWORD=$POSTGRES_PASSWORD" \
      --command -- psql --host whiteboard-db-postgresql -U whiteboard -d whiteboard -p 5432

    > NOTE: If you access the container using bash, make sure that you execute "/opt/bitnami/scripts/postgresql/entrypoint.sh /bin/bash" in order to avoid the error "psql: local user with ID 1001} does not exist"

To connect to your database from outside the cluster execute the following commands:

    kubectl port-forward --namespace database svc/whiteboard-db-postgresql 5432:5432 &
    PGPASSWORD="$POSTGRES_PASSWORD" psql --host 127.0.0.1 -U whiteboard -d whiteboard -p 5432

WARNING: The configured password will be ignored on new installation in case when previous PostgreSQL release was deleted through the helm command. In that case, old PVC will have an old password, and setting it through helm won't take effect. Deleting persistent volumes (PVs) will solve the issue.

WARNING: There are "resources" sections in the chart not set. Using "resourcesPreset" is not recommended for production. For production installations, please set the following values according to your workload needs:
  - readReplicas.resources
+info https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
```

```
kubectl create namespace whiteboard
```

```

  DB_TYPE=postgres
DB_HOST= whiteboard-db-postgresql.database.svc.cluster.local
DB_PORT=5432
DB_USERNAME=whiteboard
DB_PASSWORD=<WHITEBOARD_DB_PASSWORD>
DB_DATABASE=whiteboard
  CLIENT_BASE_URL=https://whiteboard.dungxbuif.com
  GOOGLE_CLIENT_ID=<GOOGLE_CLIENT_ID>
  GOOGLE_CLIENT_SECRET=<GOOGLE_CLIENT_SECRET>
```

```
docker build -t whiteboard-fe:latest ./excalidraw/
docker build -t whiteboard-be:latest ./selfhost-excalidraw-be/
docker build -t whiteboard-ws:latest ./ws-whiteboard/
```

```
kubectl create namespace whiteboard
**kubectl create secret -n** whiteboard  **docker-registry regcred --docker-server=https://registry.dungxbuif.com --docker-username=cicd --docker-password=<HARBOR_CICD_PASSWORD> --docker-email=cicd@dungxbuif.com**
kubectl apply -f whiteboard-configmap.yml
kubectl apply -f whiteboard-deployment.yml
kubectl apply -f whiteboard-ingress.yml

kubectl delete -f whiteboard-configmap.yml
kubectl delete -f whiteboard-deployment.yml
kubectl delete -f whiteboard-ingress.yml
```

```
kubectl get all -n whiteboard
kubectl get ingress -n whiteboard
```

### Private Registry

```
mkdir -p /opt/registry/data
chmod -R 755 /opt/registryƒ
```

```
**docker login -u cicd -p <HARBOR_CICD_PASSWORD> https://registry.dungxbuif.com/

kubectl create secret docker-registry regcred --docker-server=https://registry.dungxbuif.com --docker-username=cicd --docker-password=<HARBOR_CICD_PASSWORD> --docker-email=cicd@dungxbuif.com**

docker buildx build --platform linux/amd64 -t registry.dungxbuif.com/whiteboard/whiteboard-be:latest --load ./selfhost-excalidraw-be/
docker push registry.dungxbuif.com/whiteboard/whiteboard-be:latest

docker buildx build --platform linux/amd64 -t registry.dungxbuif.com/whiteboard/whiteboard-fe:latest --load ./excalidraw/
docker push registry.dungxbuif.com/whiteboard/whiteboard-fe:latest

docker buildx build --platform linux/amd64 -t registry.dungxbuif.com/whiteboard/whiteboard-ws:latest --load ./ws-whiteboard/
docker push registry.dungxbuif.com/whiteboard/whiteboard-ws:latest

```

```latex
helm install pgadmin runix/pgadmin4 -n database  —-values values.yml
```

## ~~CoTurn~~

```latex
kubectl apply -f namespace.yml
kubectl apply -f configmap.yml
kubectl apply -f middleware.yml
kubectl apply -f deployment.yml
kubectl apply -f service.yml
kubectl apply -f ingress.yml
kubectl apply -f hpa.yml
kubectl apply -f coturn.yml
```

```latex
kubectl delete -f namespace.yml
kubectl delete -f configmap.yml
kubectl delete -f deployment.yml
kubectl delete -f service.yml
kubectl delete -f middleware.yml
kubectl delete -f ingress.yml
kubectl delete -f hpa.yml
```

kubectl apply -f [https://raw.githubusercontent.com/kubernetes/kubernetes/v1.25.0/pkg/apis/autoscaling/v2beta2/horizontalpodautoscaler.crd.yaml](https://raw.githubusercontent.com/kubernetes/kubernetes/v1.25.0/pkg/apis/autoscaling/v2beta2/horizontalpodautoscaler.crd.yaml)

# ArgoCD

Create values from [https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml](https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml) 

```latex
kubectl create namespace argocd
kubectl apply -n argocd -f values.yml
kubectl apply -n argocd -f ingress.yml

```

Create Ingress config:

https://argo-cd.readthedocs.io/en/stable/operator-manual/ingress/#ingressroute-crd

### Disable internal TLS

update session

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/name: argocd-cmd-params-cm
    app.kubernetes.io/part-of: argocd
  name: argocd-cmd-params-cm
data:
  server.insecure: "true"
```

```yaml
kubectl apply -f values.yml
kubectl apply -f ingress.yml
helm upgrade harbor harbor/harbor -n harbor --values values.yml

```

Get admin creden tial 

```yaml
kubectl -n argocd get secret argocd-initial-admin-secret -o jsonpath="{.data.password}" | base64 -d
```

# Troubleshoot Longhon

**GitHub Copilot**

The volume degradation with `ReplicaSchedulingFailure` after removing a node is a common issue in Longhorn. This happens because Longhorn is trying to maintain the replica count but can't schedule replicas due to insufficient resources or scheduling constraints.

Let me help you diagnose and fix this issue. First, let's check the current Longhorn configuration and status:

kubectl get nodes -o wide

Now let's check the Longhorn volumes and their status:

kubectl get volumes -n longhorn-system

Let's check the Longhorn settings to see the current replica count configuration:

kubectl get settings -n longhorn-system

default-replica-count -o yaml

Great! The replica count is already set to 1, which is good for a 2-node cluster. Let's check the storage reservation setting:

kubectl get settings -n longhorn-system

storage-reserved-percentage-for-default-disk -o yaml

Good, the storage reservation is set to 15%. Now let's check the actual node and disk status in Longhorn:

kubectl get nodes.longhorn.io -n longhorn-system -o

wide

Let's check the disk status and available space:

kubectl describe nodes.longhorn.io -n longhorn-system

vps-master

kubectl describe nodes.longhorn.io -n longhorn-system

vps-worker-1

Now I can see the issue! Looking at the disk status, both nodes have replicas for all volumes, which means they have `replica-count=2` (or had it before), but now with only 2 nodes, Longhorn is trying to maintain that higher replica count and failing to schedule.

Let's check a specific volume to see its replica configuration:

kubectl describe volume -n longhorn-system

pvc-026686c9-f421-4a2d-bef4-8eea837eeb36

Perfect! Now I found the root cause. The volume has `Number Of Replicas: 3`, but you only have 2 nodes available after removing the third node. This is why Longhorn can't schedule the third replica and the volumes are showing as degraded.

Here's how to fix this issue:

## **Solution: Reduce the Replica Count**

Since you now have only 2 nodes, you need to reduce the replica count for the volumes. Here are the steps:

1. **First, let's update all degraded volumes to use 2 replicas instead of 3:**

kubectl get volumes -n longhorn-system -o name | while

read vol; do kubectl patch $vol -n longhorn-system

- -type='merge' -p '{"spec":{"numberOfReplicas":2}}';

done

1. **Now let's update the default replica count setting to 2 for future volumes:**

1. 1. **Now let's update the default replica count setting to 2 for future volumes:**

kubectl patch setting default-replica-count -n

longhorn-system --type='merge' -p '{"value":"2"}'

1. **Let's check the volume status to see if they're recovering:**

1. 1. **Let's check the volume status to see if they're recovering:**

kubectl get volumes -n longhorn-system

Excellent! 🎉 All volumes are now showing as **healthy** instead of degraded. The fix was successful!

## **What happened and why it's fixed:**

1. **Root Cause**: After removing the third node, your volumes were still configured to maintain 3 replicas, but Longhorn could only schedule 2 replicas (one on each remaining node). This caused the `ReplicaSchedulingFailure` because the third replica couldn't be scheduled.
2. **The Fix**:
    - Changed all existing volumes from 3 replicas to 2 replicas
    - Updated the default replica count setting from 1 to 2 for new volumes (2 is optimal for a 2-node cluster for redundancy)
3. **Current Status**: All volumes are now healthy with 2 replicas each, distributed across your 2 remaining nodes.

## **Additional Recommendations:**

1. **Monitor disk space**: With fewer nodes, make sure you have enough disk space. The 15% storage reservation you set should help.
2. **Consider node anti-affinity**: Your current setup is good - each volume has one replica on each node, providing redundancy.
3. **For future scaling**: If you add more nodes later, you can increase replica count if needed for additional redundancy.

Your Longhorn storage is now properly configured for your 2-node cluster and all volumes are healthy!

[https://docs.gitlab.com/charts/](https://docs.gitlab.com/charts/) 

[https://docs.gitlab.com/charts/installation/](https://docs.gitlab.com/charts/installation/)

[https://docs.gitlab.com/charts/installation/verify_cng_images/](https://docs.gitlab.com/charts/installation/verify_cng_images/)

[https://docs.gitlab.com/charts/quickstart/](https://docs.gitlab.com/charts/quickstart/)

[https://docs.gitlab.com/charts/installation/tools/](https://docs.gitlab.com/charts/installation/tools/)

[https://docs.gitlab.com/charts/installation/version_mappings/](https://docs.gitlab.com/charts/installation/version_mappings/)

[https://docs.gitlab.com/charts/installation/secrets/](https://docs.gitlab.com/charts/installation/secrets/)

[https://docs.gitlab.com/charts/installation/rbac/](https://docs.gitlab.com/charts/installation/rbac/)

[https://docs.gitlab.com/charts/installation/storage/](https://docs.gitlab.com/charts/installation/storage/)

[https://docs.gitlab.com/charts/installation/tls/](https://docs.gitlab.com/charts/installation/tls/)

[https://docs.gitlab.com/charts/installation/deployment/](https://docs.gitlab.com/charts/installation/deployment/)

[https://docs.gitlab.com/charts/installation/command-line-options/](https://docs.gitlab.com/charts/installation/command-line-options/)

[https://docs.gitlab.com/charts/charts/](https://docs.gitlab.com/charts/charts/)

[https://docs.gitlab.com/charts/charts/globals/](https://docs.gitlab.com/charts/charts/globals/)

[http://docs.gitlab.com/charts/charts/certmanager-issuer/](http://docs.gitlab.com/charts/charts/certmanager-issuer/)

[https://docs.gitlab.com/charts/charts/gitlab/](https://docs.gitlab.com/charts/charts/gitlab/)

[https://docs.gitlab.com/charts/charts/gitlab/gitaly/](https://docs.gitlab.com/charts/charts/gitlab/gitaly/)

[https://docs.gitlab.com/charts/charts/gitlab/gitlab-exporter/](https://docs.gitlab.com/charts/charts/gitlab/gitlab-exporter/)

[https://docs.gitlab.com/charts/charts/gitlab/gitlab-pages/](https://docs.gitlab.com/charts/charts/gitlab/gitlab-pages/)

[https://docs.gitlab.com/charts/charts/gitlab/gitlab-runner/](https://docs.gitlab.com/charts/charts/gitlab/gitlab-runner/)

[https://docs.gitlab.com/charts/charts/gitlab/gitlab-shell/](https://docs.gitlab.com/charts/charts/gitlab/gitlab-shell/)

[https://docs.gitlab.com/charts/charts/gitlab/kas/](https://docs.gitlab.com/charts/charts/gitlab/kas/)

[https://docs.gitlab.com/charts/charts/gitlab/mailroom/](https://docs.gitlab.com/charts/charts/gitlab/mailroom/)

[https://docs.gitlab.com/charts/charts/gitlab/migrations/](https://docs.gitlab.com/charts/charts/gitlab/migrations/)

[https://docs.gitlab.com/charts/charts/gitlab/praefect/](https://docs.gitlab.com/charts/charts/gitlab/praefect/)

[https://docs.gitlab.com/charts/charts/gitlab/sidekiq/](https://docs.gitlab.com/charts/charts/gitlab/sidekiq/)

[https://docs.gitlab.com/charts/charts/gitlab/spamcheck/](https://docs.gitlab.com/charts/charts/gitlab/spamcheck/)

[https://docs.gitlab.com/charts/charts/gitlab/toolbox/](https://docs.gitlab.com/charts/charts/gitlab/toolbox/)

[https://docs.gitlab.com/charts/charts/gitlab/webservice/](https://docs.gitlab.com/charts/charts/gitlab/webservice/)

[https://docs.gitlab.com/charts/charts/haproxy/](https://docs.gitlab.com/charts/charts/haproxy/)

[https://docs.gitlab.com/charts/charts/minio/](https://docs.gitlab.com/charts/charts/minio/)

[https://docs.gitlab.com/charts/charts/nginx/](https://docs.gitlab.com/charts/charts/nginx/)

[https://docs.gitlab.com/charts/charts/registry/](https://docs.gitlab.com/charts/charts/registry/)

[https://docs.gitlab.com/charts/charts/registry/metadata_database/](https://docs.gitlab.com/charts/charts/registry/metadata_database/)

[https://docs.gitlab.com/charts/charts/traefik/](https://docs.gitlab.com/charts/charts/traefik/)

[https://docs.gitlab.com/charts/charts/gitlab/gitlab-zoekt/](https://docs.gitlab.com/charts/charts/gitlab/gitlab-zoekt/)

[https://docs.gitlab.com/charts/charts/shared-secrets/](https://docs.gitlab.com/charts/charts/shared-secrets/)

[https://docs.gitlab.com/charts/advanced/](https://docs.gitlab.com/charts/advanced/)

[https://docs.gitlab.com/charts/advanced/custom-images/](https://docs.gitlab.com/charts/advanced/custom-images/)

[https://docs.gitlab.com/charts/advanced/external-db/](https://docs.gitlab.com/charts/advanced/external-db/)

[https://docs.gitlab.com/charts/advanced/external-db/external-omnibus-psql/](https://docs.gitlab.com/charts/advanced/external-db/external-omnibus-psql/)

[https://docs.gitlab.com/charts/advanced/external-gitaly/](https://docs.gitlab.com/charts/advanced/external-gitaly/)

[https://docs.gitlab.com/charts/advanced/external-gitaly/external-omnibus-gitaly/](https://docs.gitlab.com/charts/advanced/external-gitaly/external-omnibus-gitaly/)

[https://docs.gitlab.com/charts/advanced/external-gitlab-pages/](https://docs.gitlab.com/charts/advanced/external-gitlab-pages/)

[https://docs.gitlab.com/charts/advanced/external-mattermost/](https://docs.gitlab.com/charts/advanced/external-mattermost/)

[https://docs.gitlab.com/charts/advanced/external-nginx/](https://docs.gitlab.com/charts/advanced/external-nginx/)

[https://docs.gitlab.com/charts/advanced/external-object-storage/](https://docs.gitlab.com/charts/advanced/external-object-storage/)

[https://docs.gitlab.com/charts/advanced/external-object-storage/minio/](https://docs.gitlab.com/charts/advanced/external-object-storage/minio/)

[https://docs.gitlab.com/charts/advanced/external-object-storage/gke-workload-identity/](https://docs.gitlab.com/charts/advanced/external-object-storage/gke-workload-identity/)

[https://docs.gitlab.com/charts/advanced/external-object-storage/azure-minio-gateway/](https://docs.gitlab.com/charts/advanced/external-object-storage/azure-minio-gateway/)

[https://docs.gitlab.com/charts/advanced/external-redis/](https://docs.gitlab.com/charts/advanced/external-redis/)

[https://docs.gitlab.com/charts/advanced/external-redis/external-omnibus-redis/](https://docs.gitlab.com/charts/advanced/external-redis/external-omnibus-redis/)

[https://docs.gitlab.com/charts/advanced/fips/](https://docs.gitlab.com/charts/advanced/fips/)

[https://docs.gitlab.com/charts/advanced/internal-tls/](https://docs.gitlab.com/charts/advanced/internal-tls/)

[https://docs.gitlab.com/charts/advanced/persistent-volumes/](https://docs.gitlab.com/charts/advanced/persistent-volumes/)

[https://docs.gitlab.com/charts/advanced/ubi/](https://docs.gitlab.com/charts/advanced/ubi/)